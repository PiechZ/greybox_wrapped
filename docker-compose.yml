version: '3.4'

services:
  backend:
    image: backend
    build:
      context: ./backend/
      dockerfile: ./Dockerfile
    volumes:
      - ./data:/data
    environment:
      - SERVER=backend
  frontend:
    image: frontend
    build:
      context: ./frontend/
      dockerfile: ./Dockerfile
    ports:
      - 80:80
